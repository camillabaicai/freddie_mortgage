Unsupervised Learning Code

umap_panel_analysis.py:
  Unsupervised: Finds optimal K-means cluster (6)
  Builds Umap plots for cluster separation and default target labeling

umap_panel_prepay_analysis.py:
  Builds on K-means optimal clusters found earlier
  Builds Umap plot for prepay target labeling

lda_panel_analysis.py:
  Supervised: uses default class labels to see if class separation is feasible in 1-D KLDA space
  Builds Kernel LDA default plot

lda_panel_prepay_analysis.py:
  Supervisd: uses prepay class labels to see if class separation is feasible in 1-D KLDA space

